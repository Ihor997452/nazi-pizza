import P from '../ui/p/p';

const Footer = () => {
    return (
        <footer className="flex items-center justify-center">
            <P className="text-8xl">nazi pizza footer</P>
        </footer>
    );
};

export default Footer;
